<Window x:Class="LibraryManagement.Views.LoginApp_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LibraryManagement.ViewModels"
        mc:Ignorable="d"
        Title="LoginApp_Window" Height="350" Width="500" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent">

    <Window.DataContext>
        <vm:Login_ViewModel/>
    </Window.DataContext>

    <Border CornerRadius="20" Background="#DDA0DD   " BorderThickness="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#1A1A1B" 
                    CornerRadius="20,20,0,0" 
                    MouseDown="Border_MouseDown">
                <DockPanel LastChildFill="False">
                    <TextBlock Text="LOGIN" VerticalAlignment="Center" Foreground="White" Margin="15,0,0,0" FontSize="11" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button x:Name="btnMinimize" Content="_" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" Click="btnMinimize_Click"/>
                        <Button x:Name="btnMaximize" Content="☐" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" Click="btnMaximize_Click"/>
                        <Button x:Name="btnClose" Content="✕" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" Click="btnClose_Click"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Background="White" 
                    VerticalAlignment="Center" HorizontalAlignment="Center" 
                    CornerRadius="15" Padding="30" Width="330" Height="Auto">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Login" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#333"/>

                    <TextBlock Text="Username" Margin="0,0,0,5" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="usernameBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,15"/>

                    <TextBlock Text="Password" Margin="0,0,0,5" FontSize="12" Foreground="#666"/>
                    <PasswordBox x:Name="passBox" Height="35" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,25"/>

                    <TextBlock Text="{Binding Show_ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,5"/>

                    <Button Content="LOGIN" Command="{Binding Login_command}" CommandParameter="{Binding ElementName=passBox}" Background="#4A90E2" Foreground="White" FontWeight="Bold" Height="40" Width="100" HorizontalAlignment="Right">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>